node_modules
node_modules/
**/node_modules
**/node_modules/
dist/
ui/storybook-static/
.env
*.tsbuildinfo
drizzle/meta/
.vite/
coverage/
.DS_Store
data/
.paperclip/
.pnpm-store/
tmp-*
cli/tmp/
.vercel/

# Scratch/seed scripts (but not scripts/ dir)
check-*.mjs
!scripts/check-*.mjs
!scripts/ci/check-*.mjs
new-agent*.json
newcompany.json
seed-*.mjs
server/check-*.mjs
server/seed-*.mjs
packages/db/seed-*.mjs

# npm publish build artifacts
cli/package.dev.json

# Build artifacts in src directories
server/src/**/*.js
server/src/**/*.js.map
server/src/**/*.d.ts
server/src/**/*.d.ts.map
tmp/
feedback-export-*
diagnostics/

# Editor / tool temp files
*.tmp
.vscode/
.claude/settings.local.json
.superpowers/
.paperclip-local/
/.idea/
/.agents/

# Doc maintenance cursor
.doc-review-cursor

# Playwright
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/release-smoke/test-results/
tests/release-smoke/playwright-report/
.superset/
.claude/worktrees/

# OMC runtime state
.omc/
**/.omc/

# OMX runtime state — interview/skill state machines and the tmux hook config.
# The durable design docs OMX produces are carried into docs/superpowers/specs/
# instead (see 2026-07-28-human-agent-workforce-p0-spec.md); only the scratch
# state is ignored here.
.omx/
**/.omx/

# Stale Next.js marketing tree from v1 — only the repo-root /marketing dir is dead.
# (ui/src/marketing/ IS our active React marketing surface; keep tracking it.)
/marketing/

# Hermes / agent scratch-pad artifacts — drafted PR bodies, REPL output, etc.
# Enforced by .github/workflows/hermes-pr-audit.yml as well.
.pr_body.md
.pr-body.md
pr_body.md
pr-body.md
.scratch*
scratch.md
.draft.*
tmp/
worktrees/

# Playwright run artifacts
test-results/
tests/e2e/test-results/

# Packed MCP client, produced by `pnpm mcp:pack` and served at
# /downloads/agentdash-mcp-server.tgz. A build artifact, not a source file.
packages/mcp-server/*.tgz
server/mcp-dist/
