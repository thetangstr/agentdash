# AgentDash (#721): the PEP 517 build backend for the editable hermes-agent
# install, pinned and hash-checked. Hermes' own pyproject asks for
# setuptools==83.0.0 and an unpinned wheel; the Dockerfile installs exactly
# these into the venv and builds hermes-agent without build isolation, so no
# backend is resolved from the index at build time.
setuptools==83.0.0 --hash=sha256:29b23c360f22f414dc7336bb39178cc7bcbf6021ed2733cde173f09dba19abb3
wheel==0.45.1 --hash=sha256:708e7481cc80179af0e556bbf0cc00b8444c7321e2700b8d8580231d13017248
