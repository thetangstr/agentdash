# AgentDash 1.0: the minimal viable launch

2026-09-24 · Revised the same day with the founder's decisions (section 8) · Status: **decided scope.** The new items in section 5 are filed as #721 to #726, and every critical-path issue carries the `mvl-1.0` label.

This is a cut list, not a wishlist. It replaces the on-prem MVL that Priya and Maya drafted on the Agent Runner board (AGE-38, AGE-39, AGE-40, 2026-09-04/05) and keeps its five pillars. The product has changed shape since then. 1.0 is no longer "an MK-class firm installs AgentDash on its own Mac mini". It is **"a person's assistant drives AgentDash on a box we host"** (founder, 2026-09-23).

**Sources read:** memory notes on MKThink (customer context, production deployment, Chris-review lane, execos-local, stewardship mode, orchestration model, SaaS workstream); AGE-38/39/40 on the local runner (read-only GET); `releases/v2026.827.0.md` to `v2026.924.0.md`; `docs/guides/steward/*`; PR #653; `user-report` issues #478 to #555; the assistant MCP spec (PR #670) and #674 to #681; the SaaS discovery (PR #623, `doc/plans/2026-09-07-agentdash-saas-offering-discovery.md`); `doc/plans/2026-09-24-agent-observability-plan.md` (#694 to #700); open PRs #688, #692, #704, #705, #713 to #718; security issues #707 to #712, #719, #720; code on `origin/main` at `f536c5707`.

---

## 1. Who 1.0 is for

### What MK actually used, and what it ignored

MKThink (a strategy and design consultancy, installed on its own Mac mini on 2026-07-28, still on commit `a02aaaa4`) is the only customer evidence we have.

| Used, with evidence | Not used, or never exercised |
|---|---|
| A Chief of Staff (Casper) plus a small team: research, content, ops and client-success agents (AGE-39) | **Self-serve sign-up and the v2 CoS interview.** MK was installed on site by the founder from a runbook and a claim link (`doc/customers/mkthink/00-onsite-operating-procedure.md`). The founder-facing onboarding in section 2 has never been run by a customer |
| Directing work from a phone: the principal texts a CEO agent by iMessage (AGE-39) | **Billing.** MK is on a six-month free trial, so Stripe, caps and quotas never met a customer |
| One accountable human per agent, the steward (Titus stewards Casper), which is the `agentdash_mk` product profile | Multiple companies, lead capture (#460 to #464), connectors, the Company Evaluator (built for our own dogfooding) |
| Acting away from the board: connecting Claude Code and Codex over MCP, then asking for approvals in the harness (#544), in Teams (#542, #546) and by push (#540) | The web board as the place to work. Every late MK request was a way to *not* open it |
| Hermes agents: the steward-created agent HAL on `hermes_local` (AGE-39) | On-prem as a feature to sell. MK valued "the data never leaves the office" (AGE-39), but it also produced most of the install and upgrade pain (section 7) |

### The 1.0 customer

**A founder or operator of a 2 to 15 person team that builds software or digital product, who already delegates to a personal assistant (Muse first, Grok second), has a GitHub repo, and will bring their own model API key.** They want work done while they are away from a desk, and they want to be asked only when a decision is theirs.

This differs from MK in two honest ways. MK was a consultancy doing knowledge work, not engineering, and MK wanted on-prem. What carries over from MK is the behaviour that matters: the principal directs from a phone, stewards will not live on a board, and they bring their own model. On-prem stays available as the self-hosted path, but it is not the 1.0 launch SKU.

**Not a fit for 1.0:** anyone who needs SOC 2 or a vendor questionnaire, shared tenancy, SSO, formal per-agent stewardship, or a Muse directory listing rather than a custom connector.

### The promise

> **Tell your assistant what you want built. AgentDash's agents build it, ask you only when they must, and tell you what shipped.**

### The 1.0 shape in four lines (founder, 2026-09-24)

- **Invite-only.** An operator provisions each box by hand from a runbook. Nobody self-provisions.
- **Default product profile only.** Hosted boxes run no `agentdash_mk` profile: no stewards, no steward inbox, no bridge, no directives.
- **Hermes only.** Every hosted agent runs `hermes_local`, with a managed per-agent Hermes profile and a provider key the customer brings.
- **The built plans.** Free and Pro as built, no box fee, and idle Free boxes are suspended.

---

## 2. The 1.0 critical user journeys

Seven journeys. Each has one acceptance test. Status legend: **Built** = on `main` and exercised; **Partial** = code exists but is unmerged, unproven on a hosted box, or has a known hole; **Missing** = no code.

### CUJ-1: Get a box

*Request on agentdash.cloud, an operator provisions the box, and the founder claims it.*

| Step | Status | Evidence |
|---|---|---|
| Request a box on `www.agentdash.cloud` | **Missing** | "Start free" still signs into the orphaned Railway instance (#623 §1.1). D-S6 retires it and points the CTA at a request form (#675 comment, 2026-09-23) |
| The operator provisions the box | **Partial** (by hand) | There is no provisioning path (#623 §1.2), and 1.0 does not need one. `railway.json` and the `Dockerfile` deploy one instance. The Dockerfile's volume line was removed for Railway, so persistence needs a Railway Volume or external Postgres (`Dockerfile:90`). The runbook `doc/runbooks/hosted-box.md` is #675 scope |
| The founder claims the box | **Built** | Board-claim flow (`doc/DEPLOYMENT-MODES.md`), which makes the claimant the instance admin; invite-code sign-up guard (`server/src/middleware/invite-code-signup-guard.ts`); `FirstRunStart` (`ui/src/components/FirstRunStart.tsx`) |

**Acceptance:** an operator follows the runbook and provisions box N+1 at `<name>.agentdash.cloud` in 30 minutes or less, timed. `GET /api/health` returns `deploymentMode: authenticated` and the correct `publicBaseUrl`. The founder receives a claim link and signs in. A stranger who finds the URL cannot sign up without a code.

**#675 is re-scoped to match (D1).** Its old acceptance criterion, "a brand-new email signs up … a second signup cannot see it", put two customers on one box. That is shared tenancy, which #623 §3 rules unsafe as built and D-S1 rejects. The new criterion: one customer claims their own box, sign-up after the claim needs a code or a company invite, and each box lives in **its own Railway project**, so a server-side request forgery on one box (#709) cannot reach another box's private network.

### CUJ-2: CoS interview and first hire

| Step | Status | Evidence |
|---|---|---|
| Hermes is on the box | **Missing** | The image installs Claude Code, Codex and OpenCode (`Dockerfile:66`), not Hermes. Per-agent profiles exist (`server/src/services/hermes-profile.ts`: `provisionAgentProfile` clones a template, and each agent runs through a wrapper named `agentdash-<id>` that calls `hermes -p <profile>`), but only when `AGENTDASH_HERMES_MANAGED_PROFILES=true` (`server/src/adapters/registry.ts:134`). If provisioning fails, the run quietly falls back to the default command (`registry.ts:812-818`) |
| The founder enters a model key | **Partial** | `POST /api/onboarding/setup-adapter` (`onboarding-v2.ts:1054`) offers a `hermes` preset marked `requiresKey: false`, with the note "it holds its own provider credentials" (`services/adapter-presets.ts:76`). Nothing writes a customer's key into Hermes. The other presets write `process.env` plus "the launchd env file", which a Railway container does not keep. PR #714 limits this route to the instance admin, which is the claimant |
| CoS interview | **Built** | `/interview/turn`, `/confirm-plan`, `/revise-plan` (`onboarding-v2.ts:357,537,706`). CoS chat can dispatch through `hermes_local` (`hermes chat -q … -Q`, `services/dispatch-llm.ts:52,404`) |
| First agent hired | **Built**, with a paywall | `/agent/confirm` (`onboarding-v2.ts:438`). The Free cap is **1 agent including the CoS** (`services/tier-policy.ts:35,67`), so the first hire on Free hits the upgrade wall. **Fix, env only:** hosted boxes set `AGENTDASH_FREE_AGENT_CAP=2`, so the CoS does not use up the only Free agent. No code change |
| Default profile | **Built** | New companies are `default` (`packages/db/src/schema/companies.ts:14`). A company becomes `agentdash_mk` only with an MK invite code (`routes/companies.ts:362-395`), which hosted boxes will not issue |

**The key and Hermes' state are one persistence problem.** Hermes keeps its provider credentials, profiles, session history and its token ledger (`state.db`) under `~/.hermes`, and the per-agent wrappers under `~/.local/bin`. On Railway all of that disappears on redeploy unless it lives on the Volume. One fix covers both: mount the Volume at `/paperclip`, point `HERMES_PROFILES_DIR`, `AGENTDASH_HERMES_BIN_DIR` and the Hermes home at it, and have onboarding write the customer's key into the managed template profile there. The master key and the Hermes key directory are then covered by the same backup (S1).

**Providers.** Hermes has built-in providers for OpenRouter, Anthropic, OpenAI, xAI, Gemini, Z.AI, MiniMax, DeepSeek, Kimi and Qwen, plus any OpenAI-compatible endpoint (Hermes v0.21.2). 1.0 **supports four API-key providers**: Z.AI, which is proven on the runner (glm-5.3-flash), and OpenRouter, Anthropic and OpenAI, which get proven on the box in M5. OAuth logins (Codex, xAI, Qwen) are out: a hosted box cannot hold a consumer login. MiniMax is out because Hermes reports no reliable price or usage for it (#493).

**Acceptance:** on a fresh hosted box, the claimed founder picks a provider, pastes a key, completes the interview and confirms the plan. A CoS plus one hired agent exist, each with its own Hermes profile, and complete one test task on Free. After a redeploy, the key and both profiles are still there. No operator steps in. This runs as a Playwright spec against the box.

### CUJ-3: Connect the assistant

| Step | Status | Evidence |
|---|---|---|
| Muse connects by custom connector | **Proven on a stub** | #674 spike, outcome A: Muse takes a host plus a pre-issued client ID, PKCE S256, fixed redirect `https://agent.meta.ai/api/hatch/oauth/callback`, no `resource`, MCP 2025-06-18 |
| OAuth AS, consent, `/api/mcp/assistant`, Connections card | **Partial**: PR #688 open, 17/17 checks green | Built-in public clients (`BUILTIN_PUBLIC_CLIENTS` in `assistant-oauth.ts`), optional `resource`, a Muse 2025-06-18 handshake test, and a zero-REST-route allowlist after the security round (`.omc/briefs/devin-status.md`) |
| Grok as the second client | **Missing** | #680 (M5) |

**Acceptance:** on the launch box, Muse adds the connector, the consent screen names Muse and `agent.meta.ai`, the founder picks the company, and `whoami` answers with the company name. Grok does the same.

### CUJ-4: "Start a project to X" and agents work

| Step | Status | Evidence |
|---|---|---|
| "Who's on my team?" and "How's X going?" | **Built** | M1 merged (#685, `7b8d95d4b`): `list_team`, `list_projects`, `get_project`, `find_work`, `get_work_item` (`packages/mcp-server/src/assistant/tools.ts`) |
| `start_project`, `create_work_item`, `assign_work`, `comment_on_work`, `update_work_item` | **Missing** | #678 (M3) |
| Hermes agents pick up the work and open PRs | **Built** on the runner; **Missing** on a box | Assignment wake plus heartbeat; work products (`services/work-products.ts`); stuck-vs-quiet for Hermes (#703). Needs Hermes on the box (CUJ-2) |
| "Why is X stuck?" | **Built** (read side) | `explain_blocker` (M1). It needs OBS-1 (#704) to report real token use, and OBS-3 (#696) to say "paused by the token ceiling" |

**Acceptance:** spec §8 steps 2 to 7 pass. The project and kickoff task exist, an engineer is assigned, the activity log shows the user `via assistant_grant`, and `explain_blocker` gives the stop reason in one sentence.

### CUJ-5: "What shipped?" and "Approve that"

| Step | Status | Evidence |
|---|---|---|
| `whats_new`, `list_pending_decisions` | **Built** | M1. `list_pending_decisions` ranks approvals by risk and wait time and also returns tasks assigned to the calling person (#685 review round) |
| `prepare_decision`, `request_hire`, `confirm_action` with single-use handles | **Missing** | #679 (M4). The pattern exists in `steward-inbox-actions.ts` and `steward-inbox-decisions.ts` |

**Acceptance:** spec §8 steps 8 to 11. A decision is made with `personSaid` recorded, a replayed handle returns `ok:false`, and `whats_new` lists the PR link with correct counts. Revoking the grant makes the next call return 401 (step 12).

### CUJ-6: Invite a teammate, and they see what is waiting on them

| Step | Status | Evidence |
|---|---|---|
| Invite by email | **Built** | `POST /api/onboarding/invites` (`onboarding-v2.ts:861`) through Resend. Each box needs its own Resend key |
| Teammate joins | **Built** | `InviteLanding`, `MemberOnboarding`, resumable member onboarding (v2026.827.0) |
| "What's waiting on me?" | **Built**, without the steward inbox | The steward inbox, webhook push and bridge are `agentdash_mk` only (`steward-inbox.ts:130`, `routes/bridge.ts:61`), so they are **off** on hosted boxes. A default-profile teammate has two surfaces instead. **On the web:** `/approvals/pending` and `/approvals/:id` (`ui/src/App.tsx:172-175`, `pages/Approvals.tsx`) list every pending company approval and let them decide it. **In their assistant:** their own grant (CUJ-3), where `list_pending_decisions` returns approvals with a correct `canDecide` for default companies (fixed in the #685 review) plus tasks assigned to them |

**No new work for 1.0.** The assistant plus `/approvals` covers "what's waiting on me". What is lost is *push*: nobody is notified. The assistant is pull-based, so a person hears about a decision when they ask. That is acceptable for a 2 to 15 person team whose members already ask their assistant for a morning summary, and `whats_new` reports "N decisions wait for you". Revisit if the first customers miss decisions.

**Who may decide, on the default profile.** `requireDecisionActor` returns `null` for default companies, and the decision path reads that as `admin` (`services/approval-authority.ts:232`). So **any member of the company can decide any approval, except a hire**, which needs `agents:create` (`routes/approvals.ts:136-154`). **Accepted for 1.0**, on strangers' boxes, for four reasons:
1. A box holds one company, and only people the founder invites can sign in.
2. Those members *are* the board in the default model. That is how Paperclip's approvals were designed.
3. Hires, the decisions that create spend and new principals, already need `agents:create`.
4. An assistant decides only with the opt-in `agentdash:decide` scope, and authority is re-checked at `confirm_action` (spec §7.2).

The connect-your-assistant guide must say plainly: "every member can approve agent requests, except hires." The risk left over is a junior teammate's assistant approving an agent's external send. The mitigation is a per-grant "decisions need a tap" setting (spec §7.2, in M4), not new authority code.

**Acceptance:** the founder invites one teammate, who joins, connects their own assistant, asks "what's waiting on me?", and hears the pending approvals and their assigned tasks. The teammate can approve a non-hire request and cannot approve a hire.

### CUJ-7: Upgrade to Pro

| Step | Status | Evidence |
|---|---|---|
| Hit a cap and see the upgrade prompt | **Built** | `tier-policy.ts:57-90` (invite and hire walls); the run quota hard-blocks Free at 50 runs (`packages/shared/src/constants.ts:1087`, `heartbeat.ts:4516`) |
| 14-day no-card trial, then paid | **Built** | `payment_method_collection: "if_required"`, `trial_period_days` (`services/billing.ts:53-55`; `STRIPE_TRIAL_DAYS` defaults to 14, `app.ts:564`); checkout, portal, webhook (`routes/billing.ts:197-258`); seat sync; `BillingPage`, `PricingPage` |
| Works on a hosted box | **Missing** | Each box needs Stripe keys, price IDs and its **own webhook endpoint**. No runbook covers it |

**Acceptance:** on the launch box in Stripe test mode, the founder hits the hire wall with the third agent, starts the no-card trial, hires it, and the webhook moves `planTier` to `pro_trial`. Adding a seat changes the Stripe quantity.

---

## 3. IN for 1.0, OUT for 1.0

### IN

| Item | Why |
|---|---|
| One managed box per customer, each in its own Railway project, provisioned by an operator from a runbook (#675, re-scoped) | D-S1 and D-S4 are decided. It is the only isolation model the code supports honestly (#623 §3) |
| Invite-only access: request, operator provisioning, claim | D1 |
| **Hermes as the only runtime:** Hermes in the image, managed per-agent profiles on, all Hermes state on the Volume, and onboarding that takes a provider and key (two NEW items) | D6 as decided. Without it no agent runs on a hosted box |
| **OBS-1 (#694 / PR #704) and OBS-2 (#695 / PR #705) are launch-critical** | Hermes is the only runtime, and it meters only in its own per-profile ledger. Without OBS-1 every hosted run records 0 tokens, as 467 of 467 runs did on 3199, and then the 5M ceiling of OBS-2 can never trip |
| **OBS-3 (#696), promoted from "drop first"** | On the default profile the ceiling's pause event reaches no inbox (#705 sends it to the mk-only steward inbox), so the assistant and the agent page are the only places a person learns why an agent stopped |
| Assistant MCP M1 to M4 (#676 done, #677, #678, #679) | This is the launch gate |
| Muse by custom connector plus Grok as the second client (#680) | The spike proved the custom connector path (#674) |
| The default product profile, `/approvals`, and the assistant as the decision surface | D2 as decided. No new work (CUJ-6) |
| CoS interview and first hire, unchanged, with `AGENTDASH_FREE_AGENT_CAP=2` | Built. It only needs proving on a hosted box |
| Invites as built | The teammate journey needs them |
| Stripe Free and Pro as built, wired per box (CUJ-7) | D4. Only the per-box wiring is new |
| The security bar for hosted boxes (section 4, F1) | Hosted boxes are on the public internet and run agents that read untrusted content |
| A backup and restore drill on a hosted box, covering Postgres, the Volume (with the Hermes directory) and the secrets master key | Backups exclude uploads, workspace files and the master key (#623 §1.5) |
| A written support path | Pillar 4 |

### OUT (explicitly deferred)

| Item | Why it is out |
|---|---|
| **The `agentdash_mk` profile on hosted boxes:** stewards, the steward inbox, webhook push, the bridge and connect codes, directives, governance, deliverables, fact requests | D2 as decided. MK keeps it on-prem |
| The Teams and harness notification epic: #540, #542, #544, #546 | **The assistant replaces it,** and every piece depends on the mk steward inbox anyway |
| #553 (the inbox can never reach zero), #569 | The steward inbox is off on hosted boxes |
| #547 (one address for every audience) | A hosted box has one public HTTPS name, which removes the MK class of problem |
| Claude Code, Codex and OpenCode as hosted runtimes; OAuth-login providers; MiniMax | D6 as decided. They stay in the image for self-hosters but are not supported on hosted boxes |
| Automated provisioning, a control plane, the single-URL MCP front door, shared tenancy | #623 Phases 2 and 3. Custom connectors take a per-box URL |
| **Muse directory and Grok listings** as a launch gate (in #681 today) | D5. The custom connector works without them (#674), and Meta's review date is not ours to control. The listing work continues and does not gate 10-28 |
| On-prem OTA work: #686, #687, #347, #346, #215, #348 | Railway boxes update by redeploying a pinned image tag. These matter for MK and self-hosters, after launch |
| OBS-4, OBS-6 and OBS-7 (#697, #699, #700) | Founder decision: after launch |
| Overage charging (AGE-122), the meters reporter, `requireTier` | Pro overage stays soft-allowed and tagged (#623 §1.4). Nobody is charged for it at 1.0 |
| #691 (multiple companies as Pro-only) | One company per box. The single-workspace guard already exists (`routes/companies.ts:434`) |
| Autonomous (steward-less) mode | Parked by the founder (spec `2026-09-01-stewardship-mode-design.md`). The default profile already has no stewards |
| Company Evaluator, connectors (HubSpot, SharePoint, Gmail, Slack), lead capture #460 to #464, #701, #171 | Not used by MK and not needed by the assistant journey. Hosted boxes configure no connector OAuth clients |
| Assistant tools for deletes, budgets, pause/resume, adapter config and invites | Spec §4.2, "Not in v1" |

---

## 4. Launch-readiness bar by the five pillars

Every gate is pass or fail, with evidence attached to #681.

### Reliable
- R1. The spec §8 twelve-step script passes **three times in a row** on the hosted launch box with Muse, within 72 hours, and each run is recorded.
- R2. `tests/e2e/assistant-mcp.spec.ts` (headless SDK client) and the hosted-box journey spec (CUJ-2, CUJ-6, CUJ-7) are green on `main` and against the box.
- R3. **No open P1** labelled `mvl-1.0` on launch day.
- R4. The token ceiling (OBS-2) is on at 5M tokens per agent per day on every box, and one pause has been observed and cleared on the launch box, and reported by `explain_blocker`.
- R5. Every Hermes run on the box in the last 7 days has a `meteringStatus` of `metered`, resolved from that agent's own profile ledger. None records a silent 0 or falls back to the root ledger (OBS-1 acceptance).
- R6. Every hosted agent runs through its own `agentdash-<id>` profile wrapper. A failed profile provision fails the run instead of falling back to the shared default.

### Understandable
- U1. Every assistant answer in the three R1 runs is 600 characters or fewer and carries a deep link that resolves on `publicBaseUrl` (automated in #681).
- U2. The redaction scanner over all recorded MCP traffic finds no `pcp_`, `pcpa_`, `pcin_`, env-shaped keys, `contextSnapshot` or adapter config.
- U3. The operator can read the running release tag of each box. Priya noted on AGE-38 (2026-09-12) that health carries no build identity; check whether v2026.924.0's Instance Updates page closes that.
- U4. `agentdash.cloud` makes no claim that the build contradicts: no dollar spend figures (the demo shows "$61.40", `ui/src/marketing/demo/scenarios.ts:121`, against the tokens-not-dollars decision on #493), and no "Start free" link into the retired instance.

### Installable
- I1. Box N+1 is provisioned from `doc/runbooks/hosted-box.md` in **30 operator-minutes or less**, timed once.
- I2. The Docker image, with Hermes in it, builds on every PR (#562, merged) and the launch box runs a pinned stable image tag.
- I3. CUJ-2 passes on a fresh box with no operator help after the claim email, and the key and profiles survive a redeploy.

### Supportable
- S1. **A backup is restored on a hosted box,** covering the Postgres dump, the Volume (including the Hermes directory with its keys, profiles and ledgers) and the secrets master key. Restore time is recorded, and the restored box passes R1 steps 1 to 3.
- S2. Rollback is proven once: redeploy the previous image tag, then restore the database.
- S3. The support path is written: one support address shown as text in the app and on the site, a response target of one business day, and a runbook for the top five incidents (box down, invalid model key, ceiling or quota pause, assistant reconnect, restore). The support-access policy (D-S7: consent, time-box, audit) is decided.
- S4. The operator checks every box weekly: health, backup age, plan state and idle Free boxes. This is a runbook step, not code.

### First-customer-ready
- F1. **Security bar for hosted boxes. All of #692 and #713 to #718 are merged before the first customer box is provisioned.** In addition: #505 is fixed; #450 is closed by the hosted-box boot guard (#726) that refuses `local_trusted`; #708, #709, #710 and #719 are fixed (triage below); the #688 security review has no open HIGH or MEDIUM findings; and the `pcpa_` token reaches no raw REST route (already in #688).
- F2. `docs/guides/steward/connect-your-assistant.md` exists, states who may approve what (CUJ-6), and a first-time user follows it without help (#680).
- F3. The pricing page and Stripe prices match section 6.
- F4. **Three** named design-partner candidates who match section 1 have booked a box. MK's reference or case-study request is out (AGE-39 §4).

### Security backlog triage for 1.0

The threat model for a hosted box is set by the 1.0 shape: one customer per box, with members the founder invited, agents that read untrusted content and could be prompt-injected, a container we operate that holds the secrets master key and the customer's model key, and Railway's private network behind it.

| Issue | 1.0 | Reason |
|---|---|---|
| #708 WebSocket keeps streaming after a membership or key is revoked | **IN** (S) | Removing a departed teammate is the only offboarding control a small team has, so it has to work |
| #709 SSRF through company import preview | **IN** (S) | On a hosted box, a server-side fetch can reach cloud metadata and Railway-internal addresses. Separate Railway projects limit the blast radius; they don't close the hole |
| #710 Routine edits bypass the standing-instruction guard | **IN** (S) | It lets a prompt-injected agent rewrite future autonomous work. That is a persistence path on a box that runs untrusted content |
| #719 Members can set host-executed adapter command, env or cwd | **IN** (S) | The host is our container, holding the master key and the customer's model key. Hermes-only makes the fix simple: pin the command to the managed profile wrapper and refuse member-set command, env and cwd |
| #707 Restricted projects leak through activity, the WebSocket, export and routines | OUT | Restricted projects are an ACL *inside* one customer's team. For a 2 to 15 person team at 1.0, the guide says they are not a hard boundary. Revisit when a customer relies on them |
| #711 Member-visible metadata: private connections, secrets management, `tasks:assign_scope` | OUT | Connectors are out on hosted boxes, and secret management by members stays inside one customer. P2 |
| #712 Central visibility policy, WebSocket scoping, Postgres RLS | OUT | This is the roadmap for shared tenancy (#623 Phase 3), which 1.0 does not do |
| #720 Gmail read and draft routes use a colleague's private connection | OUT, with a condition | Hosted boxes configure no Gmail OAuth client, so the routes have no connection to misuse. It becomes IN the day connectors ship on hosted boxes |

---

## 5. The gap list, on the critical path to 2026-10-28

Sizes: S = up to 2 days, M = 3 to 5 days, L = more than a week. Owner lanes: devin (code), orchestrator (review, infra, anything that needs founder credentials), founder. Every item carries the `mvl-1.0` label.

| # | Gap | Issue | Size | Owner | Needed by |
|---|---|---|---|---|---|
| 1 | Review and merge the security fixes | #692, #713 to #718 | S (review) | orchestrator | **9/30** |
| 2 | Review and merge M2: OAuth AS and `/api/mcp/assistant` | #677 / PR #688 | S (review) | orchestrator | 9/30 |
| 3 | Review and merge OBS-1 and OBS-2 | #694 / PR #704, #695 / PR #705 | S (review) | orchestrator | 10/2 |
| 4 | **Hermes in the Docker image:** pinned Hermes, Hermes home and wrappers on the Volume, `AGENTDASH_HERMES_MANAGED_PROFILES=true`, fail closed when a profile cannot be provisioned, OBS-1 resolves every run to its profile ledger | #721 | M | devin | **10/3** |
| 5 | **The launch box and runbook,** re-scoped: its own Railway project, Railway Postgres or an external `DATABASE_URL`, a Volume at `/paperclip`, the master key as a Railway variable, `publicBaseUrl`, a pinned image tag, the Muse built-in client, sign-up closed except by code or invite, `AGENTDASH_FREE_AGENT_CAP=2`, retire the old instance after export (D-S6), DNS and TLS | #675 | L | orchestrator (founder: Railway token, DNS) | **10/6** |
| 6 | **Onboarding takes a Hermes provider and API key** (Z.AI, OpenRouter, Anthropic, OpenAI) and writes it into the managed template profile on the Volume, replacing the launchd env-file path on hosted boxes | #725 | M | devin | 10/8 |
| 7 | Hosted-box boot guard: with a hosted flag set, refuse `local_trusted`, require `PAPERCLIP_PUBLIC_URL` and managed Hermes profiles (closes #450) | #726 | S | devin | 10/8 |
| 8 | Security fixes: #505, #708, #709, #710, #719 | existing | 5 × S | devin | 10/10 |
| 9 | M3 work tools | #678 | M | devin | 10/12 |
| 10 | M4 gated actions, including "decisions need a tap" | #679 | M | devin | 10/15 |
| 11 | OBS-3: token and ceiling fields in `whats_new` and `explain_blocker` | #696 | S | devin | 10/15 |
| 12 | Per-box Stripe and Resend: keys, price IDs and a webhook endpoint per box; CUJ-7 in test mode on the box | #722 | S | orchestrator (founder: Stripe account) | 10/15 |
| 13 | Backup, restore and rollback drill on the box, Hermes directory included (S1, S2) | inside #675 | S | orchestrator | 10/15 |
| 14 | Hosted-box journey spec (Playwright): CUJ-2, CUJ-6, CUJ-7 against a real box | #723 | M | devin | 10/16 |
| 15 | M5 conformance on the launch box: Muse, Grok and Claude, the four providers, and the connect-your-assistant guide | #680 | M | devin | 10/21 |
| 16 | Support path, weekly box check, site truth and the request-a-box CTA (S3, S4, U4) | #724 | S | orchestrator + founder | 10/21 |
| 17 | M6 launch run: three consecutive §8 passes and the redaction scanner. **Listings are removed from the gate** | #681 | M | devin | 10/27 |

**Critical path:** security merges (9/30) → Hermes image #721 (10/3) → launch box #675 (10/6) → Hermes onboarding #725 (10/8) → M3 (10/12) → M4 (10/15) → M5 (10/21) → M6 (10/27) → launch 10/28.

### Does 2026-10-28 hold?

**Only with two code lanes. On one serial Devin lane it slips about a week, to around 2026-11-04.**

- **Ahead:** M1 merged 9/24. M2, OBS-1 and OBS-2 are open with green CI.
- **Added by D2 and D6:** Hermes in the image (M) and Hermes onboarding (M) join the critical path, because the box cannot run an agent without them. The launch box itself (#675) moves from 10/2 to 10/6 because it waits on the Hermes image.
- **Added by the security bar:** six PRs to review before any customer box, and five small fixes.
- **The arithmetic:** Devin's queue is now about 23 working days of serial work (items 4, 6, 7, 8, 9, 10, 11, 14, 15, 17). There are about 23 working days between 9/25 and 10/27, with no buffer left.
- **What makes it hold:** run a second Devin session, or another engineer, on the platform track (items 4, 6, 7, 8 and 14) while the main lane does M3 → M4 → M5 → M6. With two lanes, the platform track finishes by 10/10 and M6 keeps its buffer.
- **Not in our control:** Meta's directory review, which D5 took off the path.

---

## 6. Pricing and packaging for 1.0

Use what is built (D4). The only new billing work is per-box wiring (gap 12).

| | **Free** | **Pro** |
|---|---|---|
| Price | $0 | **$29 per seat per month** (`PricingPage.tsx:242,429`) |
| Humans | 1 (`FREE_HUMAN_CAP`) | Unlimited, synced to seat count |
| Agents | **2 on hosted boxes**: the CoS plus one hire (`AGENTDASH_FREE_AGENT_CAP=2`; the code default is 1 including the CoS) | Unlimited |
| Runs | 50 per month, then a hard block | 1,000 plus 250 per seat, then soft overage that is tagged but not charged |
| Assistant connection | Yes | Yes |
| Model cost | The customer's own provider key | The customer's own provider key |

**How a box moves through the plans:** every hosted box starts on Free. The second hire past the CoS, or the first teammate invite, is the upgrade moment. It starts the **14-day no-card Pro trial** through Stripe checkout. When the trial lapses without a card, the box drops back to Free caps, as the code already does. **Operator rule, not code:** a Free box idle for 14 days is suspended and exported, because every box costs Railway spend while Free earns nothing.

**Not doing for 1.0:** a box fee, managed inference, value-event pricing (#623 D-S2, D-S3), overage charges, or annual plans.

**One number to watch:** Free (2 agents, 50 runs a month) cannot run the §8 script, which needs a CoS and two engineers and many runs. That is intended: the script's setup is a Pro-trial box.

---

## 7. What MK taught us that changes the product

1. **Stewards will not live on a board; they will live in their assistant.** Four of MK's open requests are ways to decide without opening the web UI: #540, #542, #544 and #546. So did the iMessage CEO agent (AGE-39) and the in-harness inbox (#662). → The assistant is the product surface, and the Teams epic is cut.
2. **The address was broken more often than the system.** `INVALID_ORIGIN` from a wrong public port cost a day (#483). Approval links used `127.0.0.1` (#539). `.local` names fail over a VPN (AGE-5, #547). The connect page's default address was reversed on field evidence (PR #653). → One public HTTPS name per box, and every link is built from `publicBaseUrl` (spec §5).
3. **"Configured" is not "applied".** Hermes agents ran with no mandate (AGE-8, #554). Directives were stored and never injected (AGE-2). The UI reported the wrong model (AGE-1). Agents were born with heartbeat off (v2026.902.0). → Every 1.0 surface reports what actually ran, and a hosted agent that cannot get its own Hermes profile fails instead of running on a shared one (R6).
4. **Metering was blind, and it cost a day of outage.** 467 of 467 runs on 3199 recorded 0 tokens, while one no-op heartbeat session burned about 10M input tokens a day and exhausted the Z.AI weekly quota on 2026-09-21 (observability plan §3). → With Hermes as the only runtime, OBS-1 and the 5M ceiling are launch-critical. We show tokens, never dollars.
5. **Install and upgrade are the product for a small team.** The first install took 90 to 120 founder minutes on site. The v2026.827.2 upgrade was rehearsed with the verdict NO-PROMOTION, and MK is still on its July commit (AGE-39, `doc/customers/MINIMUM-REPEATABLE-DEPLOYMENT.md` §5). → Managed boxes where we own the upgrade. On-prem becomes the self-hosted path.
6. **Questions that reach no one become silent decisions.** The Hermes clarify channel reached nobody, and a 120-second timeout let the agent decide by default (AGE-13, fixed fail-closed in #571). → Every question an agent asks surfaces in `list_pending_decisions`, and gated actions are enforced on the server, not trusted to the client (spec §7.2; Muse runs its tool calls as Python in a sandbox, #674).
7. **State that lies erodes trust faster than missing features.** An inbox that can never reach zero (#553). Agents created by agents that were "stewarded" with no steward (#550). A 403 reported as "this person does not exist" (#552). A PATCH that silently discarded `steward` (#551). → Pillar 2 gates are about truth, not new features.
8. **The product profile was a hidden fork.** MK ran `agentdash_mk`, while a company without an MK code gets `default`, where the workforce surfaces return 404 "with nothing explaining why" (`FirstRunStart.tsx:11`). → 1.0 settles it: hosted boxes are `default` only, and the MK stack stays on-prem.

---

## 8. Founder decisions (2026-09-24)

1. **D1, accepted.** Invite-only launch. An operator provisions each box by hand from a runbook, and the founder claims it by email. #675 is re-scoped (CUJ-1).
2. **D2, reversed from the recommendation.** Hosted boxes run the **default profile only**, with no `agentdash_mk`. Consequences: no steward inbox, so teammates use `/approvals` plus their assistant (CUJ-6), and the approval authority of any member except for hires is accepted (CUJ-6). The ceiling pause needs OBS-3 to be visible (section 3).
3. **D3, accepted.** Fix #505 (agents see names and roles, not emails). Close #450 by refusing `local_trusted` on hosted boxes. Merge #692. The security bar (F1) adds #713 to #718 before the first customer box, and #708, #709, #710 and #719 before launch.
4. **D4, accepted.** The built plans, no box fee, and idle Free boxes are suspended. `AGENTDASH_FREE_AGENT_CAP=2` on hosted boxes, so the CoS does not use up the only Free agent.
5. **D5, accepted.** Directory listings do not gate the launch. Muse launches by custom connector.
6. **D6, reversed from the recommendation.** **Hermes is the only hosted runtime.** Consequences: Hermes goes in the image, every agent gets its own managed profile, all Hermes state and the customer's key live on the Volume, onboarding takes a provider and key for Z.AI, OpenRouter, Anthropic or OpenAI, and OBS-1 and OBS-2 become launch-critical.

**One decision still open:** a second code lane for the platform track (section 5), without which 10-28 slips about a week.

*Already decided, not re-asked:* Muse gates the launch with Grok second; one box per customer; one Railway service per box; retire the old instance; assistants may decide with the opt-in `decide` scope; OBS-1 and the 5M ceiling before launch; tokens, never dollars; stewardship mode stays parked.
