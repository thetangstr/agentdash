# Upstream digest — 2026-05-02

Snapshot of `upstream/master` against this branch. Generated by `scripts/upstream-digest.sh`.
See [doc/UPSTREAM-POLICY.md](../UPSTREAM-POLICY.md) for the cherry-pick rubric.

## Summary

| Bucket | Count | What it means |
|---|---|---|
| Worth a look (score ≥ 2) | 0 | Touches files we still inherit. Review for cherry-pick. |
| Skip — agentdash-owned | 0 | Touches code we own; upstream has no fix here. |
| Skip — conflict-only wiring | 0 | Only touches App.tsx / Sidebar / app.ts / shared constants. |
| Skip — other (tests, docs, unrelated) | 0 | Doesn't touch any classified path. |
| **Total upstream commits ahead** | **0** | (sum equals total) |

## Action

If a row above looks worth taking, cherry-pick it per the rubric in
[doc/UPSTREAM-POLICY.md](../UPSTREAM-POLICY.md):

```sh
git cherry-pick <sha>
pnpm -r typecheck && pnpm test:run && pnpm build
# Then log the cherry-pick in doc/UPSTREAM-POLICY.md.
```

If nothing above looks worth taking, this digest is the receipt — close the loop and move on.
