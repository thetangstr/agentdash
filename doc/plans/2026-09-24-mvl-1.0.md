# AgentDash 1.0: the minimal viable launch

2026-09-24 · Status: **proposal for founder approval.** No issues are filed and nothing is built by this document. The "NEW" items in section 5 become issues only after the founder approves them.

This is a cut list, not a wishlist. It replaces the on-prem MVL that Priya and Maya drafted on the Agent Runner board (AGE-38, AGE-39, AGE-40, 2026-09-04/05) and keeps its five pillars. The product has changed shape since then. 1.0 is no longer "an MK-class firm installs AgentDash on its own Mac mini". It is **"a person's assistant drives AgentDash on a box we host"** (founder, 2026-09-23).

**Sources read:** memory notes on MKThink (customer context, production deployment, Chris-review lane, execos-local, stewardship mode, orchestration model, SaaS workstream); AGE-38/39/40 on the local runner (read-only GET); `releases/v2026.827.0.md` to `v2026.924.0.md`; `docs/guides/steward/*`; PR #653; `user-report` issues #478 to #555; the assistant MCP spec (PR #670) and #674 to #681; the SaaS discovery (PR #623, `doc/plans/2026-09-07-agentdash-saas-offering-discovery.md`); `doc/plans/2026-09-24-agent-observability-plan.md` (#694 to #700); open PRs #688, #692, #704, #705; code on `origin/main` at `f536c5707`.

---

## 1. Who 1.0 is for

### What MK actually used, and what it ignored

MKThink (a strategy and design consultancy, installed on its own Mac mini on 2026-07-28, still on commit `a02aaaa4`) is the only customer evidence we have.

| Used, with evidence | Not used, or never exercised |
|---|---|
| A Chief of Staff (Casper) plus a small team: research, content, ops and client-success agents (AGE-39) | **Self-serve sign-up and the v2 CoS interview.** MK was installed on site by the founder from a runbook and a claim link (`doc/customers/mkthink/00-onsite-operating-procedure.md`). The founder-facing onboarding in section 2 has never been run by a customer |
| Directing work from a phone: the principal texts a CEO agent by iMessage (AGE-39) | **Billing.** MK is on a six-month free trial, so Stripe, caps and quotas never met a customer |
| One accountable human per agent, the steward (Titus stewards Casper), which is the `agentdash_mk` product profile | Multiple companies, lead capture (#460 to #464), connectors, the Company Evaluator (built for our own dogfooding) |
| Acting away from the board: connecting Claude Code and Codex over MCP, then asking for approvals in the harness (#544), in Teams (#542, #546) and by push (#540) | The web board as the place to work. Every late MK request was a way to *not* open it |
| Agents filing their own bug reports (#478 to #480) | On-prem as a feature to sell. MK valued "the data never leaves the office" (AGE-39), but it also produced most of the install and upgrade pain (section 7) |

### The 1.0 customer

**A founder or operator of a 2 to 15 person team that builds software or digital product, who already delegates to a personal assistant (Muse first, Grok second), has a GitHub repo, and will bring their own model API key.** They want work done while they are away from a desk, and they want to be asked only when a decision is theirs.

This differs from MK in two honest ways. MK was a consultancy doing knowledge work, not engineering, and MK wanted on-prem. What carries over from MK is the behaviour that matters: the principal directs from a phone, stewards will not live on a board, and they bring their own model. On-prem stays available as the self-hosted path, but it is not the 1.0 launch SKU.

**Not a fit for 1.0:** anyone who needs SOC 2 or a vendor questionnaire, shared tenancy, SSO, or a Muse directory listing rather than a custom connector.

### The promise

> **Tell your assistant what you want built. AgentDash's agents build it, ask you only when they must, and tell you what shipped.**

---

## 2. The 1.0 critical user journeys

Seven journeys. Each has one acceptance test. Status legend: **Built** = on `main` and exercised; **Partial** = code exists but is unmerged, unproven on a hosted box, or has a known hole; **Missing** = no code.

### CUJ-1: Get a box

*Request on agentdash.cloud, a box is provisioned, and the founder claims it.*

| Step | Status | Evidence |
|---|---|---|
| Request a box on `www.agentdash.cloud` | **Missing** | "Start free" still signs into the orphaned Railway instance (#623 §1.1). D-S6 retires it and points the CTA at a waitlist or invite flow (#675 comment, 2026-09-23) |
| A box is provisioned | **Missing** (automated) / **Partial** (by hand) | There is no provisioning path (#623 §1.2). `railway.json` and the `Dockerfile` deploy one instance; the Dockerfile's volume line was removed for Railway, so persistence needs a Railway Volume or external Postgres (`Dockerfile:90`). There is no runbook yet: `doc/runbooks/hosted-box.md` is #675 scope |
| The founder claims the box | **Built** | Board-claim flow (`doc/DEPLOYMENT-MODES.md`), invite-code gate (`server/src/routes/invite-codes.ts`), `FirstRunStart` (`ui/src/components/FirstRunStart.tsx`) |

**Acceptance:** an operator follows the runbook and provisions box N+1 at `<name>.agentdash.cloud` in 30 minutes or less, timed. `GET /api/health` returns `deploymentMode: authenticated` and the correct `publicBaseUrl`. The founder receives a claim link and signs in.

**Scope correction.** #675's acceptance criterion "a brand-new email signs up … a second signup cannot see it" puts two customers on one box. That is shared tenancy, which #623 §3 rules unsafe as built and which D-S1 (one box per customer) rejects. For 1.0, sign-up means **claiming your own box**, not self-serve bootstrap on a shared one (Decision D1).

### CUJ-2: CoS interview and first hire

| Step | Status | Evidence |
|---|---|---|
| Model key entered | **Partial** | `POST /api/onboarding/setup-adapter` (`server/src/routes/onboarding-v2.ts:1054`) hot-sets `process.env` and persists to "the launchd env file". A Railway container has no launchd and loses that file on redeploy unless it is on a volume. Adapters installed in the image: Claude Code, Codex, OpenCode (`Dockerfile:66`). **Hermes is not installed**, even though every MK and runner agent uses it |
| CoS interview | **Built** | `/interview/turn`, `/confirm-plan`, `/revise-plan` (`onboarding-v2.ts:357,537,706`); `ui/src/pages/CoSConversation.tsx` |
| First agent hired | **Built**, with a paywall | `/agent/confirm` (`onboarding-v2.ts:438`), `agent-creator-from-proposal.ts`. The Free cap is **1 agent including the CoS** (`server/src/services/tier-policy.ts:35,67`), so on Free the first hire hits the upgrade wall |
| Product profile | **Partial** | New companies default to `productProfile: "default"` (`packages/db/src/schema/companies.ts:14`). Stewards, the steward inbox, the bridge, directives and the mk approval authority work only in `agentdash_mk` (`steward-inbox.ts:130`, `approval-authority.ts:69`, `routes/bridge.ts:61`; 44 non-test files). MK ran `agentdash_mk`. On a managed box a company becomes `agentdash_mk` only with a code from `AGENTDASH_MK_INVITE_CODES` (`routes/companies.ts:362-395`, `lib/mk-invite-codes.ts`); otherwise it is `default`, which turns off directives, governance and deliverables |

**Acceptance:** on a fresh hosted box, the claimed founder enters a model key, completes the interview, confirms the plan, and a CoS plus one hired agent exist and complete one test task. No operator steps in. This runs as a Playwright spec against the box.

### CUJ-3: Connect the assistant

| Step | Status | Evidence |
|---|---|---|
| Muse connects by custom connector | **Proven on a stub** | #674 spike, outcome A: Muse takes a host plus a pre-issued client ID, PKCE S256, fixed redirect `https://agent.meta.ai/api/hatch/oauth/callback`, no `resource`, MCP 2025-06-18 |
| OAuth AS, consent, `/api/mcp/assistant`, Connections card | **Partial**: PR #688 open, 17/17 checks green | Built-in public clients (`BUILTIN_PUBLIC_CLIENTS` in `assistant-oauth.ts`), optional `resource`, a Muse 2025-06-18 handshake test, and a zero-REST-route allowlist after the security round (`.omc/briefs/devin-status.md`) |
| Grok as the second client | **Missing** | #680 (M5) |

**Acceptance:** on the launch box, Muse adds the connector, the consent screen names Muse and `agent.meta.ai`, the founder picks the company, and `whoami` answers with the company name. Grok does the same.

### CUJ-4: "Start a project to X" and agents work

| Step | Status | Evidence |
|---|---|---|
| "Who's on my team?" and "How's X going?" | **Built** | M1 merged (#685, `7b8d95d4b`): `list_team`, `list_projects`, `get_project`, `find_work`, `get_work_item` (`packages/mcp-server/src/assistant/tools.ts`) |
| `start_project`, `create_work_item`, `assign_work`, `comment_on_work`, `update_work_item` | **Missing** | #678 (M3) |
| Agents pick up the work and open PRs | **Built** | Assignment wake plus heartbeat; work products (`services/work-products.ts`); the reviewer auto-hire is now approval-gated (#683, v2026.924.0) |
| "Why is X stuck?" | **Built** (read side) | `explain_blocker` (M1). Richer with OBS-1 (#704, open) |

**Acceptance:** spec §8 steps 2 to 7 pass. The project and kickoff task exist, an engineer is assigned, the activity log shows the user `via assistant_grant`, and `explain_blocker` gives the stop reason in one sentence.

### CUJ-5: "What shipped?" and "Approve that"

| Step | Status | Evidence |
|---|---|---|
| `whats_new`, `list_pending_decisions` | **Built** | M1 |
| `prepare_decision`, `request_hire`, `confirm_action` with single-use handles | **Missing** | #679 (M4). The pattern exists in `steward-inbox-actions.ts` and `steward-inbox-decisions.ts` |

**Acceptance:** spec §8 steps 8 to 11. A decision is made with `personSaid` recorded, a replayed handle returns `ok:false`, and `whats_new` lists the PR link with correct counts. Revoking the grant makes the next call return 401 (step 12).

### CUJ-6: Invite a teammate and give them their inbox

| Step | Status | Evidence |
|---|---|---|
| Invite by email | **Built** | `POST /api/onboarding/invites` (`onboarding-v2.ts:861`) through Resend. Each box needs its own Resend key |
| Teammate joins | **Built** | `InviteLanding`, `MemberOnboarding`, resumable member onboarding (v2026.827.0) |
| Steward inbox | **Built** for `agentdash_mk` only | v2026.904.0; webhook push (v2026.922.0/.1); guides in the app (v2026.923.0). A teammate's own assistant grant is CUJ-3 again, with one grant per person |

**Acceptance:** the founder invites one teammate, who joins, connects their own assistant, and sees only decisions they have authority over.

### CUJ-7: Upgrade to Pro

| Step | Status | Evidence |
|---|---|---|
| Hit a cap and see the upgrade prompt | **Built** | `tier-policy.ts:57-90` (invite and hire walls); run quota hard-blocks Free at 50 runs (`packages/shared/src/constants.ts:1087`, `heartbeat.ts:4516`) |
| 14-day no-card trial, then paid | **Built** | `payment_method_collection: "if_required"`, `trial_period_days` (`services/billing.ts:53-55`, `STRIPE_TRIAL_DAYS` default 14 in `app.ts:564`); checkout, portal, webhook (`routes/billing.ts:197-258`); seat sync; `BillingPage`, `PricingPage` |
| Works on a hosted box | **Missing** | Each box needs Stripe keys, price IDs and its **own webhook endpoint**. No runbook covers it |

**Acceptance:** on the launch box in Stripe test mode, the founder hits the hire wall, starts the no-card trial, hires the second engineer, and the webhook moves `planTier` to `pro_trial`. Adding a seat changes the Stripe quantity.

---

## 3. IN for 1.0, OUT for 1.0

### IN

| Item | Why |
|---|---|
| One managed box per customer, provisioned by an operator from a runbook (#675, re-scoped) | D-S1 and D-S4 are decided. It is the only isolation model the code supports honestly (#623 §3) |
| Invite-only access: request, then operator provisioning, then claim | We can provision boxes by hand in 30 minutes or less. Self-serve provisioning is #623 Phase 2 |
| Assistant MCP M1 to M4 (#676 done, #677, #678, #679) | This is the launch gate |
| Muse by custom connector plus Grok as the second client (#680) | The spike proved the custom connector path (#674) |
| One product profile on hosted boxes: `agentdash_mk` (Decision D2) | It is what MK proved: stewards, approval authority, inbox |
| CoS interview and first hire, unchanged (CUJ-2) | Built. It only needs proving on a hosted box |
| Invites, the steward inbox, and webhook push as built | Built. The teammate journey needs them |
| Stripe Free and Pro as built, wired per box (CUJ-7) | Built. Only the per-box wiring is new |
| OBS-1 honest per-run record (#694 / PR #704) and OBS-2 token ceiling of 5M per agent per day (#695 / PR #705) | Founder decision, 2026-09-24. The Z.AI quota outage of 2026-09-21 is the proof |
| Security: #692 (anonymous conversation reads), #505 fixed, #450 closed with a hosted-mode assertion, #688's security review closed | Hosted boxes are on the public internet and run agents that read untrusted content |
| A backup and restore drill on a hosted box, including the secrets master key and volume | Backups exclude uploads, workspace files and the master key (#623 §1.5) |
| A written support path | Pillar 4 |

### OUT (explicitly deferred)

| Item | Why it is out |
|---|---|
| The Teams and harness notification epic: #540, #542, #544, #546 | **The assistant replaces it.** Each is an MK request to decide away from the board, and `list_pending_decisions` plus `confirm_action` answer all of them in one surface. The Teams webhook that shipped (v2026.922.x) stays as built, with no new work |
| #553 (the inbox can never reach zero) | The assistant, not the web inbox, is the 1.0 decision surface. Revisit if stewards use the web inbox on hosted boxes |
| #547 (one address for every audience) | A hosted box has one public HTTPS name, which removes the MK class of problem |
| Automated provisioning, a control plane, the single-URL MCP front door, shared tenancy | #623 Phases 2 and 3. Custom connectors take a per-box URL |
| **Muse directory and Grok listings** as a launch gate (in #681 today) | The custom connector works without them (#674). Meta's review date is not ours to control. The listing work continues, but it does not gate 10-28 (Decision D5) |
| Hermes and GLM on hosted boxes | The image does not ship Hermes, and its metering needed OBS-1 to work at all. 1.0 hosted adapters are Claude Code and Codex with the customer's API key (Decision D6) |
| On-prem OTA work: #686, #687, #347, #346, #215, #348 | Railway boxes update by redeploying a pinned image tag. These matter for MK and self-hosters, after launch |
| OBS-3 (#696) if it slips; OBS-4, OBS-6 and OBS-7 (#697, #699, #700) | Founder decision: OBS-3 is dropped first if the launch path slips; the rest comes after launch |
| Overage charging (AGE-122), the meters reporter, `requireTier` | Pro overage stays soft-allowed and tagged (#623 §1.4). Nobody is charged for it at 1.0 |
| #691 (multiple companies as Pro-only) | One company per box. The single-workspace guard already exists (`routes/companies.ts:434`) |
| Autonomous (steward-less) mode | Parked by the founder (spec `2026-09-01-stewardship-mode-design.md`) |
| Company Evaluator, connectors (HubSpot, SharePoint, Gmail), lead capture #460 to #464, #701, #171 | Not used by MK and not needed by the assistant journey |
| Assistant tools for deletes, budgets, pause/resume, adapter config and invites | Spec §4.2, "Not in v1" |

---

## 4. Launch-readiness bar by the five pillars

Every gate is pass or fail, with evidence attached to #681.

### Reliable
- R1. The spec §8 twelve-step script passes **three times in a row** on the hosted launch box with Muse, within 72 hours, and each run is recorded.
- R2. `tests/e2e/assistant-mcp.spec.ts` (headless SDK client) and the CUJ-2 onboarding spec are green on `main` and against the box.
- R3. **No open P1** labelled for the launch path (`mcp-launch` or `hosted`) on launch day.
- R4. The token ceiling (OBS-2) is on at 5M tokens per agent per day on every box, and one pause has been observed and cleared on the launch box.
- R5. Every run on the box in the last 7 days has a `meteringStatus`. None records a silent 0 (OBS-1 acceptance).

### Understandable
- U1. Every assistant answer in the three R1 runs is 600 characters or fewer and carries a deep link that resolves on `publicBaseUrl` (automated in #681).
- U2. The redaction scanner over all recorded MCP traffic finds no `pcp_`, `pcpa_`, `pcin_`, env-shaped keys, `contextSnapshot` or adapter config.
- U3. The running release tag of each box can be read by the operator. Priya noted on AGE-38 (2026-09-12) that health carries no build identity; check whether v2026.924.0's Instance Updates page closes that.
- U4. `agentdash.cloud` makes no claim that the build contradicts: no dollar spend figures (the demo shows "$61.40", `ui/src/marketing/demo/scenarios.ts:121`, against the tokens-not-dollars decision on #493), and no "Start free" link into the retired instance.

### Installable
- I1. Box N+1 is provisioned from `doc/runbooks/hosted-box.md` in **30 operator-minutes or less**, timed once.
- I2. The Docker image builds on every PR (#562, merged) and the launch box runs a pinned stable image tag.
- I3. CUJ-2 passes on a fresh box with no operator intervention after the claim email.

### Supportable
- S1. **A backup is restored on a hosted box**, including the Postgres dump, the volume and the secrets master key. Restore time is recorded and the restored box passes R1 steps 1 to 3.
- S2. Rollback is proven once: redeploy the previous image tag, then restore the database.
- S3. The support path is written: one support address shown as text in the app and on the site, a response target of one business day, and a runbook for the top five incidents (box down, invalid model key, ceiling or quota pause, assistant reconnect, restore). The support-access policy (D-S7: consent, time-box, audit) is decided.
- S4. The operator has a weekly check of every box that confirms health, backup age and plan state. This is a runbook step, not code.

### First-customer-ready
- F1. **Security:** #692 merged; #505 fixed; #450 closed with a hosted-mode readiness assertion (the box refuses `local_trusted`); the #688 security review closed with no open HIGH or MEDIUM findings; the `pcpa_` token reaches no raw REST route (already in #688).
- F2. `docs/guides/steward/connect-your-assistant.md` exists and a first-time user follows it without help (#680).
- F3. The pricing page and Stripe prices match section 6.
- F4. **Three** named design-partner candidates who match section 1 have booked a box. MK's reference or case-study request is out (AGE-39 §4).

---

## 5. The gap list, on the critical path to 2026-10-28

Sizes: S = up to 2 days, M = 3 to 5 days, L = more than a week. Owner lanes follow the 2026-09-23 routing: devin, runner, orchestrator, founder. Security work never goes to the runner (model-routing policy).

| # | Gap | Issue | Size | Owner | Needed by |
|---|---|---|---|---|---|
| 1 | Founder decisions D1 to D6 (section 8) | NEW (decisions) | S | founder | **9/26** |
| 2 | Review and merge M2: OAuth AS and `/api/mcp/assistant` | #677 / PR #688 | S (review) | orchestrator | 9/30 |
| 3 | Review and merge the conversations company wall | #692 | S | orchestrator | 9/30 |
| 4 | Review and merge OBS-1 and OBS-2 | #694 / #704, #695 / #705 | S | orchestrator | 10/2 |
| 5 | **The launch box and runbook**, re-scoped to per-customer claim: Railway service, Railway Postgres or external `DATABASE_URL`, a Volume at `/paperclip`, the master key and model key held as Railway variables (not the launchd env file), `publicBaseUrl`, a pinned image tag, the Muse built-in client, retiring the old instance after export (D-S6), DNS and TLS | #675 | L | orchestrator (founder: Railway token and DNS) | **10/2** |
| 6 | Hosted companies are created as `agentdash_mk` (the runbook hands the claimant an MK invite code, or the box defaults to it); check that one founder with a CoS and 2 engineers is not blocked by the one-steward-per-user rule (a trap seen on 3199 on 2026-09-02) | NEW | M | devin | 10/9 |
| 7 | Fix #505: agents get member id, name and role but no email | #505 | S | orchestrator | 10/9 |
| 8 | M3 work tools | #678 | M | devin | 10/12 |
| 9 | M4 gated actions | #679 | M | devin | 10/15 |
| 10 | Per-box Stripe and Resend: keys, price IDs, a webhook endpoint per box; CUJ-7 in test mode on the box | NEW | S | orchestrator (founder: Stripe account) | 10/15 |
| 11 | Backup and restore drill plus rollback drill on the box (S1, S2) | inside #675, called out | S | orchestrator | 10/15 |
| 12 | Playwright spec for CUJ-2, CUJ-6 and CUJ-7 against a hosted box | NEW | M | runner (test-writing only) | 10/16 |
| 13 | M5 conformance on the launch box: Muse, Grok and Claude; the connect-your-assistant guide | #680 | M | devin | 10/21 |
| 14 | Support path and weekly box check (S3, S4); site truth (U4); request-a-box CTA | NEW | S | founder + orchestrator | 10/21 |
| 15 | M6 launch run: three consecutive §8 passes and the redaction scanner. **Listings removed from the gate** | #681 | M | devin | 10/27 |
| 16 | OBS-3 token fields in `whats_new` and `explain_blocker` (dropped first) | #696 | S | devin | if it fits |

**Critical path:** decisions (9/26) → the launch box (#675, 10/2) → M3 → M4 → M5 → M6. M2 is no longer on it: PR #688 is a week ahead of the spec's 9/30 to 10/8 window.

### Does 2026-10-28 hold?

**Yes, as an invite-only launch on Muse with concierge-provisioned boxes. No, as open self-serve sign-up or as a Muse directory launch.**

- **Ahead:** M1 merged 9/24. M2, OBS-1 and OBS-2 are open with green CI.
- **The long pole is the hosted box (#675).** It has not started. The Railway MCP was unreachable on 9/24. It needs a Railway token and DNS from the founder, and it has the persistence problems listed in item 5. If it slips past 10/2, M5 slips with it, and each week of slip moves launch a week, as the spec says (§11).
- **Devin's lane is serial:** M3 → M4 → M5 → M6 plus item 6. Four days of buffer (10/22 to 10/27) sit inside M6. Moving the reviews (items 2 to 4) and the #505 fix to the orchestrator keeps Devin on the path.
- **Not in our control:** the Muse directory review. Removing listings from the gate (D5) takes it off the path.

---

## 6. Pricing and packaging for 1.0

Use what is built. The only new billing work is per-box wiring (gap 10).

| | **Free** | **Pro** |
|---|---|---|
| Price | $0 | **$29 per seat per month** (`PricingPage.tsx:242,429`) |
| Humans | 1 (`FREE_HUMAN_CAP`) | Unlimited, synced to seat count |
| Agents | 1, **including the CoS** (`FREE_AGENT_CAP`) | Unlimited |
| Runs | 50 per month, then a hard block | 1,000 plus 250 per seat, then soft overage that is tagged but not charged |
| Assistant connection | Yes | Yes |
| Model cost | The customer's own key | The customer's own key |

**How a box moves through the plans:** every hosted box starts on Free. The first hire past the CoS, or the first teammate invite, is the upgrade moment. It starts the **14-day no-card Pro trial** through Stripe checkout. When the trial lapses without a card, the box drops back to Free caps, as the code already does. **Operator rule, not code:** a Free box idle for 14 days is suspended and exported, because every box costs us Railway spend while Free earns nothing.

**Not doing for 1.0:** a box fee, managed inference, value-event pricing (#623 D-S2, D-S3), overage charges, or annual plans.

**One number to watch:** the free tier (1 agent including the CoS, 50 runs) cannot run the §8 script, which needs a CoS and two engineers. That is intended: the script's setup is a Pro-trial box. `tier-policy.ts:24-28` recommends caps of 2 humans and 3 agents for a launch, through env variables. Keep the defaults unless the founder wants Free to demo the whole team (D4).

---

## 7. What MK taught us that changes the product

1. **Stewards will not live on a board; they will live in their assistant.** Four of MK's open requests are ways to decide without opening the web UI: #540, #542, #544 and #546. So did the iMessage CEO agent (AGE-39) and the in-harness inbox (#662). → The assistant is the product surface, and the Teams epic is cut.
2. **The address was broken more often than the system.** `INVALID_ORIGIN` from a wrong public port cost a day (#483). Approval links used `127.0.0.1` (#539). `.local` names fail over a VPN (AGE-5, #547). The connect page's default address was reversed on field evidence (PR #653). → One public HTTPS name per box, and every link is built from `publicBaseUrl` (spec §5).
3. **"Configured" is not "applied".** Hermes agents ran with no mandate (AGE-8, #554). Directives were stored and never injected (AGE-2). The UI reported the wrong model (AGE-1). Agents were born with heartbeat off (v2026.902.0). → Every 1.0 surface reports what actually ran, and the assistant's `explain_blocker` reads real stop reasons.
4. **Metering was blind, and it cost a day of outage.** 467 of 467 runs on 3199 recorded 0 tokens, while one no-op heartbeat session burned about 10M input tokens a day and exhausted the Z.AI weekly quota on 2026-09-21 (observability plan §3). Hermes reports no reliable price for MiniMax (#493). → OBS-1 and the 5M ceiling are launch gates. We show tokens, never dollars.
5. **Install and upgrade are the product for a small team.** The first install took 90 to 120 founder minutes on site. The v2026.827.2 upgrade was rehearsed with the verdict NO-PROMOTION, and MK is still on its July commit (AGE-39, `doc/customers/MINIMUM-REPEATABLE-DEPLOYMENT.md` §5). → Managed boxes where we own the upgrade. On-prem becomes the self-hosted path.
6. **Questions that reach no one become silent decisions.** The Hermes clarify channel reached nobody, and a 120-second timeout let the agent decide by default (AGE-13, fixed fail-closed in #571). → Every question an agent asks surfaces in `list_pending_decisions`, and gated actions are enforced on the server, not trusted to the client (spec §7.2; Muse runs its tool calls as Python in a sandbox, #674).
7. **State that lies erodes trust faster than missing features.** An inbox that can never reach zero (#553). Agents created by agents that were "stewarded" with no steward (#550). A 403 reported as "this person does not exist" (#552). A PATCH that silently discarded `steward` (#551). → Pillar 2 gates are about truth, not new features.
8. **The customer's product profile was a hidden fork.** MK ran `agentdash_mk`, while a managed-box sign-up without an MK code creates `default`, where the workforce surfaces return 404 "with nothing explaining why" (`FirstRunStart.tsx:11`). → 1.0 ships one profile (D2).

---

## 8. Founder decisions needed (by 2026-09-26)

1. **D1. Invite-only launch with concierge boxes.** 1.0 means: request on the site, an operator provisions from the runbook, the founder claims the box by email. Re-scope #675 to drop the "second signup on the same box" criterion. *Recommend: yes.*
2. **D2. Hosted boxes run the `agentdash_mk` profile only.** It is the stewardship, approval-authority and inbox stack that MK proved. The v2 onboarding must create `agentdash_mk` companies on hosted boxes (gap 6). *Recommend: yes.*
3. **D3. Security posture for strangers' boxes.** Fix #505 before launch: agents see member names and roles, not emails, because agents read untrusted content and can send mail. Close #450 as by-design for `local_trusted`, and add a hosted-box assertion that refuses that mode. Merge #692. *Recommend: yes to all three.*
4. **D4. Free on a hosted box.** Keep the built caps (1 human, 1 agent including the CoS, 50 runs) with the trial as the upgrade path, and suspend idle Free boxes after 14 days. Alternative: set the code's recommended launch caps of 2 humans and 3 agents. No box fee at 1.0. *Recommend: built caps, no box fee, revisit at 10 boxes.*
5. **D5. Take listings out of the launch gate.** Launch on the Muse custom connector the spike proved. The Muse directory and Grok listings, and the legal entity behind them, continue in parallel and do not gate 10-28. *Recommend: yes.*
6. **D6. Hosted adapters are Claude Code and Codex with the customer's API key.** Hermes and GLM stay on-prem and on the runner. They are not in the image and not supported on hosted boxes at 1.0. *Recommend: yes.*

*Already decided, not re-asked:* Muse gates the launch with Grok second; one box per customer; one Railway service per box; retire the old instance; assistants may decide with the opt-in `decide` scope; OBS-1 and the 5M ceiling before launch; tokens, never dollars; stewardship mode stays parked.
